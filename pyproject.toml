[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "cycle_tracker"
version = "0.1.0"
description = "A CLI tool for tracking menstrual cycles"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []